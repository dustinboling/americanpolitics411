<% if @people.count == 0 %>
<h2>Select from the options above.</h2>
<% else %>
<table id="officials-table">
  <thead>
    <tr>
      <th>Name</th>
      <th>Party</th>
    </tr>
  </thead>
  <tbody>
    <% @people.each do |p| %>
    <tr>
      <td><%= link_to p.name, person_path(p.id.to_slug) %></td>
      <td><%= p.current_party %></td>
    </tr>
    <% end %>
  </tbody>
</table>
<% end %>
