<%= form_for(@university, :html => { :class => 'form-stacked' }) do |f| %>
<%= error_messages_for(@university) %>


    <%= f.label :name %>
    <%= f.text_field :name %>

    <%= f.submit :class => 'btn success' %>

<% end %>
