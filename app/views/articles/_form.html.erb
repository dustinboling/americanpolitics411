<%= form_for(@article) do |f| %>
  <% if @article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@article.errors.count, "error") %> prohibited this article from being saved:</h2>

      <ul>
      <% @article.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :person, 'Person ID' %><br />
    <%= f.select(:person_id, options_for_select(@people.collect {|u| [u.last_name + ", " + u.first_name, u.id]}, @person.id))%>
  </div>
  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :excerpt %><br />
    <%= f.text_area :excerpt %>
  </div>
 <div class="field">
    <%= f.label :article_url %><br />
    <%= f.text_field :article_url %>
  </div>
	<div class="field">
		<%= link_to('Add an Attachment', {:controller => 'attachments', :action => 'new', :person_id => @person.id}) %>
	</div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>