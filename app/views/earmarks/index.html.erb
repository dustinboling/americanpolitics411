<h1>Listing earmarks</h1>

<table>
  <tr>
    <th>Person</th>
    <th>Organization</th>
    <th>Description</th>
    <th>Sector</th>
    <th>Amount</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @earmarks.each do |earmark| %>
  <tr>
    <td><%= earmark.person_id %></td>
    <td><%= earmark.organization_id %></td>
    <td><%= earmark.description %></td>
    <td><%= earmark.sector %></td>
    <td><%= earmark.amount %></td>
    <td><%= link_to 'Show', earmark %></td>
    <td><%= link_to 'Edit', edit_earmark_path(earmark) %></td>
    <td><%= link_to 'Destroy', earmark, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to '<< Back to People', people_path %>
