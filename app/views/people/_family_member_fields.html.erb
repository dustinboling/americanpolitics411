<div class="fields actions">
	<%= f.hidden_field :person_id, :value => @person.id %>
	<p>
    <%= f.label :family_member, 'Related to' %>
    <%= f.select(:family_member, options_for_select(@people.collect {|fm| [fm.full_name, fm.id]}, f.object.family_member.to_s)) %> 
		<%= link_to('Add a new person.', {:controller => 'people', :action => 'new'})  %>
		
  </p>
  <p>
    <%= f.label :relationship %>
    <%= f.text_field :relationship %>
  </p>
  <p>
    <%= f.label :notes %>
    <%= f.text_field :notes %>
  </p>
	<span class="label important">
		<%= link_to_remove_fields "remove family member", f %>
	</span>
</div>
