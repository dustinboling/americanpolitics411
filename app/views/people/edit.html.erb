<div>
  <h1 class="page-title">Editing: <%= @person.first_name %> <%= @person.last_name %></h1>

  <div id="form-details">
    <%= render 'form-edit' %>
  </div>

  <%= link_to 'Show', @person %> |
  <%= link_to 'Back', people_path %>

</div>
