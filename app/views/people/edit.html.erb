<div class="container">
	<h1>Editing: <%= @person.first_name %> <%= @person.last_name %></h1>

	<div id ="tabs" class="span4">
		<div id="admin-navigation-side">
			<%= render 'admin-navigation-side' %>
		</div>
		<div id="form-details" class="span12">
			<%= render 'form-edit' %>
		</div>

		<%= link_to 'Show', @person %> |
		<%= link_to 'Back', people_path %>

	</div> <!-- /#tabs -->
</div>