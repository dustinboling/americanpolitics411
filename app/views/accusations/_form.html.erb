<%= form_for(@accusation) do |f| %>
  <% if @accusation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@accusation.errors.count, "error") %> prohibited this accusation from being saved:</h2>

      <ul>
      <% @accusation.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :person_id %><br />
    <%= f.select(:person_id, options_for_select(@people.collect {|u| [u.last_name + ", " + u.first_name, u.id]}, @person.id))%>
  </div>
	<div class="field">
    <%= f.label :date, "Year" %><br />
    <%= f.date_select(:date, :start_year => 1951) %>
  </div>
	<div class="field">
    <%= f.label :accusation %><br />
    <%= f.text_area :accusation %>
  </div>
	<div class="field">
    <%= f.label :outcome %><br />
    <%= f.text_area :outcome %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
