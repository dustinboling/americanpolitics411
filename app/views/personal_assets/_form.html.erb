<%= form_for(@personal_asset) do |f| %>
  <% if @personal_asset.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@personal_asset.errors.count, "error") %> prohibited this personal asset from being saved:</h2>

      <ul>
      <% @personal_asset.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :organization %><br />
    <%= f.select(:organization_name, @organizations.collect {|t| [t.name]}, :prompt => :true) %>
  </div>
  <div class="field">
    <%= f.label :person_id %><br />
    <%= f.select(:person_id, options_for_select(@people.collect {|u| [u.last_name + ", " + u.first_name, u.id]}, @person.id))%>
  </div>
  <div class="field">
    <%= f.label :date, "Year" %><br />
    <%= f.date_select(:date, :start_year => 1951) %>
  </div>
	<div class="field">
    <%= f.label :value, 'Value (Actual)' %><br />
    <%= f.text_field :value %>
  </div>
	<div class="field">
    <%= f.label :value, 'Value (Minimum Estimate)' %><br />
    <%= f.text_field :value_min %>
  </div>
	<div class="field">
    <%= f.label :value, 'Value (Maximum Estimate)' %><br />
    <%= f.text_field :value_max %>
  </div>
	<div class="field">
    <%= f.label :action, 'Sold' %><br />
    <%= f.radio_button :action, "0" %>
  </div>
	<div class="field">
    <%= f.label :action, 'Purchased' %><br />
		<%= f.radio_button :action, "1" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
