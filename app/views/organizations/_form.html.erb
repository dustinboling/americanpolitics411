<%= form_for(@organization, :html => { :class => "form-stacked" }) do |f| %>
<%= error_messages_for(@organization) %>

	<%= f.label :name %>
	<%= f.text_field :name %>

	<%= f.submit :class => 'button' %>

<% end %>
