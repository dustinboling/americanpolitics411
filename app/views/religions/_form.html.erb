	<%= form_for(@religion, :html => { :class => 'form-stacked' }) do |f| %>
	<%= error_messages_for(@religion) %>

	    <%= f.label :name %><br />
	    <%= f.text_field :name %>

	    <%= f.submit :class => 'button'%>

	<% end %>
